<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is
         starting. Renders before any Flutter code runs, so it must be
         driven entirely by Android resources. We paint the whole
         window with `splash_background` (deep purple-black) — the
         Flutter SplashView picks up from this exact colour, making the
         hand-off invisible.

         The previous LaunchTheme was generated by `flutter_native_splash`;
         the package and its `launch_background` drawable have been
         removed in favour of this simpler, single source of truth. -->
    <style name="LaunchTheme" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- Theme applied as soon as the process has started, behind the
         Flutter UI. Same dark colour for visual continuity. -->
    <style name="NormalTheme" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
