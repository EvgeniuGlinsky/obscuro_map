name: obscuro_map
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  go_router: ^17.1.0
  go_router_builder: ^4.2.0
  flutter_svg: ^2.2.3
  vector_graphics: ^1.1.19
  latlong2: ^0.9.1
  flutter_bloc: ^9.1.1
  get_it: ^9.2.1
  flutter_meta_sdk: ^1.0.5
  injectable_generator: ^2.11.1
  injectable: ^2.7.1+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.1
  flutter_gen_runner: ^5.2.0
  google_maps_flutter: ^2.5.3
  flutter_map: ^6.1.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/features/splash/

flutter_gen:
  output: lib/gen/
  line_length: 120
  integrations:
    flutter_svg: true
    image: true
    rive: true

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/launcher/app_logo.svg"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/launcher/app_logo.svg"